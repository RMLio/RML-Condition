package be.ugent.mmlab.rml.condition.processor;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

import org.openrdf.model.Resource;
import org.openrdf.model.URI;

/**
 * RML - Conditions
 * 
 * Performer to do joins without any join conditions
 *
 * @author mielvandersande, andimou
 */
/*public class BindRMLPerformer  {

    // Log
    private static final Logger log = 
            LogManager.getLogger(BindRMLPerformer.class);
    private Resource subject;
    private URI predicate;

    /**
     *
     * @param processor
     * @param subject
     * @param predicate
     */
    /*public BindRMLPerformer(Resource subject, URI predicate) {
        this.subject = subject;
        this.predicate = predicate;
    }
}*/
